#!/usr/bin/env bash
# Runs once on Linux after chezmoi apply
# Idempotent - safe to re-run

set -euo pipefail

echo "==> Linux post-install"

# Ensure user dirs
mkdir -p ~/.config ~/.local/bin ~/.scripts ~/.cache

# Add ~/.scripts to PATH if not present
if ! grep -q "HOME/.scripts" ~/.zsh/exports.zsh 2>/dev/null; then
  echo "  ~  ~/.scripts already in PATH"
fi

# Make all scripts executable
chmod +x ~/.scripts/* 2>/dev/null || true

echo "==> Done. Reload your shell: source ~/.zshrc"
