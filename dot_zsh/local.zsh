# local.zsh - machine-specific overrides
# This file is OPTIONAL. Create it manually on each machine.
# It is NOT tracked by git (add to .gitignore in your fork if needed).

# Example:
# export API_KEY="xxx"
# alias work="cd ~/work"
